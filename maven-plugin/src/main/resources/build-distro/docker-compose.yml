# Generated automatically generated by openmrs SDK
version: '2'

services:
  openmrs-<distro>-mysql:
    image: mysql:5.6
    container_name: openmrs-<distro>-mysql
    command: "mysqld --character-set-server=utf8 --collation-server=utf8_general_ci"
    environment:
      MYSQL_DATABASE: openmrs
      MYSQL_ROOT_PASSWORD: Admin123
      MYSQL_USER: openmrs
      MYSQL_PASSWORD: Admin123
    volumes:
      - ./dbdump:/docker-entrypoint-initdb.d # Remove this volume to have an empty database when starting
      # - openmrs-<distro>-mysql-data:/var/lib/mysql  # uncomment to keep database between docker runs

  openmrs-<distro>:
    container_name: openmrs-<distro>
    build: <distro>-docker-image  # change to 'image' is using an image deployed to dockerhub
    depends_on:
      - openmrs-<distro>-mysql
    ports:
      - "8080:8080"
    environment:
      DB_DATABASE: openmrs
      DB_HOST: openmrs-<distro>-mysql
      DB_USERNAME: openmrs
      DB_PASSWORD: Admin123
      DB_CREATE_TABLES: 'false'  # change to 'true' if using an empty database
      DB_ADD_DEMO_DATA: 'false'  # change to 'true' if using an empty database
      DB_AUTO_UPDATE: 'false'    # change to 'true' if using an empty database

volumes:
  openmrs-<distro>-mysql-data:
