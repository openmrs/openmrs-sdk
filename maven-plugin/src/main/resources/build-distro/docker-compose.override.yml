# Generated automatically generated by openmrs SDK
version: '2'

services:
  web:
    environment:
      DEBUG: 'true'
    ports:
      - "${TOMCAT_PORT}:8080"
      - "${DEBUG_PORT}:1044"

  db:
    ports:
      - "${MYSQL_PORT}:3306"
